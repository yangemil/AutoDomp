<div class="page">
    <div class="page-header">
        <h1>{{project.name}}</h1>
        <div class="action-buttons">
            <button class="btn btn-primary" onclick="createTestCase()">+ 创建测试用例</button>
            <button class="btn btn-secondary" onclick="editProject()">编辑项目</button>
        </div>
    </div>

    <div class="project-info">
        <div class="info-card">
            <h3>项目信息</h3>
            <div class="info-row">
                <span class="label">描述：</span>
                <span class="value">{{project.description}}</span>
            </div>
            <div class="info-row">
                <span class="label">Base URL：</span>
                <span class="value"><code>{{project.baseUrl}}</code></span>
            </div>
            <div class="info-row">
                <span class="label">创建时间：</span>
                <span class="value">{{formatDate project.createdAt}}</span>
            </div>
        </div>

        <div class="info-card">
            <h3>测试配置</h3>
            <div class="info-row">
                <span class="label">无界面模式：</span>
                <span class="value">{{#if project.settings.headless}}是{{else}}否{{/if}}</span>
            </div>
            <div class="info-row">
                <span class="label">慢速模式：</span>
                <span class="value">{{project.settings.slowMo}}ms</span>
            </div>
            <div class="info-row">
                <span class="label">超时时间：</span>
                <span class="value">{{project.settings.timeout}}ms</span>
            </div>
            <div class="info-row">
                <span class="label">截图：</span>
                <span class="value">{{#if project.settings.screenshot.enabled}}启用{{else}}禁用{{/if}}</span>
            </div>
        </div>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-value">{{testCases.length}}</div>
            <div class="stat-label">测试用例</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{executions.length}}</div>
            <div class="stat-label">执行记录</div>
        </div>
        <div class="stat-card success">
            <div class="stat-value">{{filterCount executions 'passed'}}</div>
            <div class="stat-label">通过</div>
        </div>
        <div class="stat-card danger">
            <div class="stat-value">{{filterCount executions 'failed'}}</div>
            <div class="stat-label">失败</div>
        </div>
    </div>

    <div class="section">
        <h2>最近测试用例</h2>
        {{#if testCases.length}}
        <table class="table">
            <thead>
                <tr>
                    <th>名称</th>
                    <th>URL</th>
                    <th>创建时间</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                {{#each testCases}}
                <tr>
                    <td>{{this.name}}</td>
                    <td><code>{{this.url}}</code></td>
                    <td>{{formatDate this.createdAt}}</td>
                    <td class="actions">
                        <button class="btn btn-sm" onclick="executeTest('{{this.id}}')">执行</button>
                        <button class="btn btn-sm btn-secondary" onclick="editTestCase('{{this.id}}')">编辑</button>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <div class="empty-state">
            <p>暂无测试用例</p>
            <button class="btn btn-primary" onclick="createTestCase()">创建第一个测试用例</button>
        </div>
        {{/if}}
    </div>
</div>

<script>
const projectId = '{{project.id}}';

function createTestCase() {
    window.location.href = `/test-cases/${projectId}/create`;
}

function editProject() {
    window.location.href = `/projects`;
}

function editTestCase(testCaseId) {
    window.location.href = `/test-cases/${projectId}/${testCaseId}/edit`;
}

async function executeTest(testCaseId) {
    if (!confirm('确定要执行这个测试用例吗？')) {
        return;
    }

    try {
        const response = await fetch(`/api/execute/${testCaseId}`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({})
        });
        const result = await response.json();

        if (result.success) {
            alert(`测试执行完成！状态: ${result.execution.status}`);
            window.location.reload();
        } else {
            alert('执行失败: ' + result.error);
        }
    } catch (error) {
        alert('请求失败: ' + error.message);
    }
}
</script>

<style>
.project-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    margin-bottom: 30px;
}

.info-card {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    padding: 24px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.5);
}

.info-card h3 {
    margin-bottom: 20px;
    font-size: 18px;
    color: #1f2937;
    padding-bottom: 12px;
    border-bottom: 2px solid #3b82f6;
    font-weight: 600;
}

.info-row {
    display: flex;
    margin-bottom: 14px;
    font-size: 14px;
    align-items: center;
}

.info-row .label {
    font-weight: 600;
    min-width: 100px;
    color: #6b7280;
}

.info-row .value {
    flex: 1;
    color: #1f2937;
}

.info-row code {
    background: rgba(243, 244, 246, 0.8);
    padding: 4px 10px;
    border-radius: 6px;
    font-size: 13px;
    color: #dc2626;
    border: 1px solid rgba(229, 231, 235, 0.5);
}

.section {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    padding: 24px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.5);
    margin-bottom: 20px;
}

.section h2 {
    margin-bottom: 20px;
    font-size: 18px;
    color: #1f2937;
    font-weight: 600;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-bottom: 30px;
}

.stat-card {
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.9) 0%, rgba(118, 75, 162, 0.9) 100%);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    padding: 24px;
    border-radius: 16px;
    text-align: center;
    color: white;
    box-shadow: 0 4px 20px rgba(102, 126, 234, 0.3);
    transition: transform 0.2s, box-shadow 0.2s;
    border: 1px solid rgba(255, 255, 255, 0.3);
}

.stat-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(102, 126, 234, 0.4);
}

.stat-card.success {
    background: linear-gradient(135deg, rgba(17, 153, 142, 0.9) 0%, rgba(56, 239, 125, 0.9) 100%);
    box-shadow: 0 4px 20px rgba(56, 239, 125, 0.3);
}

.stat-card.success:hover {
    box-shadow: 0 8px 30px rgba(56, 239, 125, 0.4);
}

.stat-card.danger {
    background: linear-gradient(135deg, rgba(235, 51, 73, 0.9) 0%, rgba(244, 92, 67, 0.9) 100%);
    box-shadow: 0 4px 20px rgba(235, 51, 73, 0.3);
}

.stat-card.danger:hover {
    box-shadow: 0 8px 30px rgba(235, 51, 73, 0.4);
}

.stat-value {
    font-size: 36px;
    font-weight: 700;
    margin-bottom: 8px;
    color: #ffffff !important;
}

.stat-label {
    font-size: 14px;
    opacity: 0.9;
    font-weight: 500;
    color: #ffffff !important;
}

@media (max-width: 768px) {
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}
</style>
